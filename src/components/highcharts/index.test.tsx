/*
import * as React from 'react';
import renderer from 'react-test-renderer';
import ActivityContent from '.';

it('renders with activity prop and without name', () => {
    const activity = {
    }
    const tree = renderer
        .create(<ActivityContent activity={activity} />)
        .toJSON();
    expect(tree).toMatchSnapshot();
});

it('renders with activity prop and with name', () => {
    const activity = {
        object: 'text'
    }
    const tree = renderer
        .create(<ActivityContent activity={activity} />)
        .toJSON();
    expect(tree).toMatchSnapshot();
});
*/