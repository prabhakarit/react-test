import React from 'react';
import Highcharts from 'highcharts';
import HighchartsReact from 'highcharts-react-official';
/*
const options = {
    chart: {
      type: 'spline'
    },
    title: {
      text: 'My chart'
    },
    series: [
      {
        data: [1, 2, 1, 4, 3, 6]
      }
    ]
  };

interface Props {
  chart;
  title;
  series;
}

const ChartsHigh: React.FC<Props> = props => {
    const options = Object.assign(props.chart, props.title, props.series);
    return (
        <div>
            <HighchartsReact highcharts={Highcharts} options={options} />
        </div>
    );
};

export default ChartsHigh;
*/